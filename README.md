# وفّري

ماركتبليس تجميع للعروسة في مصر: من الخطوبة لفستان الفرح لأجهزة البيت ورفايع حمام التلات. مش محل بيبيع الجهاز — طبقة مقارنة فوق المتاجر المتصلة، وخريطة للأحياء اللي السعر بيتقارن فيها على الطبيعة.

## خريطة الشراء

[`/map`](http://127.0.0.1:43147/map) فيها **21 مرحلة**: خطوبة، فستان، إكسسوار، مكياج، عناية، لانجري، لبس، أحذية وشنط، شهر عسل، أجهزة، مطبخ، رفايع مطبخ، مفروشات، حمام، تنظيف، تنظيم، ديكور، سفرة، عطور بيت، هدايا، طوارئ.

المصادر تشمل وان ستوب (صيدناوي، سيتي ستايل، جوميا، نون، أمازون، كارفور، هايبر وان، سبينيس، كازيون، مترو، التوحيد والنور) وأحياء الجملة (عبدالعزيز، العتبة، حمام التلات، شبرا، النزهة، الموسكي…) وبوتيكات الفساتين والصاغة والصيدليات.

الأحياء في حالة **تواصل**: ظاهرة على الخريطة من غير أسعار ملفّقة. الربط الحي للأونلاين بأفلييت أو فيد تاجر.

## دليل المنتجات

[`/guide`](http://127.0.0.1:43147/guide) قايمة بنود + بوكسات جاهزة للجهاز.

## أماكن على الخريطة

[`/places`](http://127.0.0.1:43147/places): أحياء الجملة (حمام التلات، عبدالعزيز، العتبة، دمياط…) مع تلميح «غالبًا أرخص»، خريطة، ولينك خرائط جوجل. لو `GOOGLE_MAPS_API_KEY` موجود المحلات تيجي من Google Places.

## فيد المصدر والأفلييت

[`/ingest`](http://127.0.0.1:43147/ingest): سحب CSV/JSON من رابط فيد مصرّح، ظهور اسم المصدر على كل عرض، ولينك خروج بأفلييت وكوبون تظبطيهم لكل متجر. صفحات HTML للمتاجر مش بتتسحب.

## البحث

`/search` هجين:

1. منتجات وفّري (جهاز العروسة والبيت).
2. نتايج ويب بنفس كروت وفّري.

لو `GOOGLE_API_KEY` و `GOOGLE_CSE_ID` موجودين، النتايج من **Google Programmable Search**. من غير المفتاح بنستخدم فهرس مفتوح (ويكيبيديا + ملخصات ويب + خرائط) — مش سكرابينج لصفحة جوجل.

انسخِي `.env.example` لـ `.env.local` بعد ما تعملي محرك بحث على [programmablesearchengine.google.com](https://programmablesearchengine.google.com) وتفعّلي «البحث في الويب كله».

## التشغيل محليًا

```bash
npm install
npm run dev
```

افتح [http://127.0.0.1:43147](http://127.0.0.1:43147).

## الصفحات

| المسار | الوظيفة |
| --- | --- |
| `/` | الرئيسية |
| `/map` | خريطة 21 مرحلة |
| `/places` | أماكن وأحياء على الخريطة |
| `/guide` | دليل البنود والبوكسات |
| `/search` | بحث هجين: كتالوج وفّري + ويب بشكل وفّري |
| `/stores` | شبكة المصادر |
| `/list` | قايمة الجهاز |
| `/ingest` | سحب فيد المصدر + أفلييت وكوبونات |

قائمة 300–500 SKU بهامش ربح لمحل فعلي مش جزء من الطبقة دي — دي خريطة مصادر + مقارنة، مش كتالوج جملة للبيع.

## المكدس

Next.js + TypeScript + Tailwind + shadcn/ui.
